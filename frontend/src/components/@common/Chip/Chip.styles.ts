import styled from 'styled-components';

const Root = styled.div`
  width: 60px;
  height: 24px;
  border-radius: 25px;
  font-size: 12px;
  font-weight: 500;
  text-align: center;
`;

export default { Root };
