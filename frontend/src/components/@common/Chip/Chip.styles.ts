import styled from 'styled-components';

const Root = styled.div`
  width: 4.25rem;
  height: 1.5rem;
  border-radius: 1rem;
  font-size: 0.75rem;
  font-weight: 500;
  text-align: center;
`;

export default { Root };
